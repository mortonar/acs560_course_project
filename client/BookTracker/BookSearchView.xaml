<UserControl x:Class="BookTracker.BookSearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BookTracker"
             mc:Ignorable="d" Height="300" Width="450">
    <Grid>
        <TextBlock Text="Book Search" FontWeight="Bold" HorizontalAlignment="Center" Margin="69,5,69,10" FontSize="30" />
        <TextBlock Text="Author" HorizontalAlignment="Center" Margin="10,20,155,0" FontSize="15" Width="72" Grid.Row="2" Grid.Column="1"/>
        <TextBox x:Name="txtAuthor" Text="{Binding BookSearchModel.Author}" Margin="100,0,20,0" MinWidth="100" HorizontalAlignment="Center" Grid.Row="2" Grid.Column="1" Width="110"/>
        <TextBlock Text="Title" HorizontalAlignment="Center" Margin="10,20,155,0" FontSize="15" Width="72" Grid.Row="1" Grid.Column="1"/>
        <TextBox x:Name="txtTitle" Text="{Binding BookSearchModel.Title}" Margin="100,0,20,0" MinWidth="100" HorizontalAlignment="Center" Grid.Row="1" Grid.Column="1" Width="110" />
        <Button Command="{Binding ''}" Content="Book Search" Margin="10,5,10,5" Grid.Row="3" Grid.Column="1"/>
    </Grid>
</UserControl>
