<UserControl x:Class="BookTracker.Views.BookView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BookTracker.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20*"/>
            <RowDefinition Height="100*"/>
        </Grid.RowDefinitions>
        <Image Source="{Binding Path=BookModel.Image}" Width="100" Grid.Row="1" Grid.Column="2"/>
        <TextBlock Text="{Binding Path=BookModel.Listing}" FontSize="20" Grid.Column="2"/>
        <TextBlock Text="{Binding Path=BookModel.Description}" FontSize="20" TextWrapping="Wrap" Grid.Column="1" Grid.Row="3"/>
    </Grid>
</UserControl>
